<view class="loading-container {{fullscreen ? 'fullscreen' : ''}}">
  <view class="loading-spinner">
    <view class="spinner-dot" wx:for="{{[1,2,3]}}" wx:key="*this"></view>
  </view>
  <text class="loading-text" wx:if="{{text}}">{{text}}</text>
</view>
